/*const numeroItems = 2;
for (let i = 0;i < numeroItems; i++){
  let ul= window.getElementById('lista');
  let li = window.createElement('li');
  li.appendChild(window.createTextNode(`Item ${i}`));
  ul.appendChild(li);
}*/